# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/airpal?schema=public"

# JWT — use a long random secret in production
JWT_SECRET="change-me-to-a-long-random-secret"
JWT_EXPIRES_IN="7d"
